{
  "name": "event-portal-fullstack",
  "version": "1.0.0",
  "scripts": {
    "frontend-install": "npm install --prefix frontend",
    "backend-install": "npm install --prefix backend",
    "install-all": "npm run frontend-install && npm run backend-install",
    "frontend-build": "npm run build --prefix frontend",
    "start": "node backend/server.js",
    "render-postbuild": "npm run frontend-install && npm run frontend-build && npm run backend-install"
  },
  "description": "A full-stack MERN application for discovering and managing college/public events.",
  "main": "index.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
